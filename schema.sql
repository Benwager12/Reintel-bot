-- Create server prefix db
CREATE TABLE SERVER_PREFIX (
	SERVER_ID VARCHAR(18) NOT NULL PRIMARY KEY,
	PREFIX VARCHAR(3) NOT NULL
);

-- Create timer db
CREATE TABLE USER_TIMERS (
	ID SERIAL PRIMARY KEY NOT NULL,
	USER_ID VARCHAR(18) NOT NULL,
	END_DATE BIGINT NOT NULL,
	SERVER_ID VARCHAR(18) NOT NULL
);

-- Create todo db
CREATE TABLE USER_TODO (
	ID SERIAL PRIMARY KEY NOT NULL,
	USER_ID VARCHAR(18) NOT NULL,
	SERVER_ID VARCHAR(18) NOT NULL,
	MESSAGE VARCHAR(250) NOT NULL,
	TIME_ADDED BIGINT NOT NULL
);

INSERT INTO USER_TODO (USER_ID, SERVER_ID, MESSAGE, TIME_ADDED)
VALUES ('104244519838416896', '979482043878625330', 'Hey!', 1658009431);

INSERT INTO USER_TODO (USER_ID, SERVER_ID, MESSAGE, TIME_ADDED)
VALUES ('104244519838416896', '979482043878625330', 'Hey 2!', 1658009524);